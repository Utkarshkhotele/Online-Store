<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- DB Connection -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/online_store</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">Utkarsh</property>

        <!-- Hibernate settings -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Hide SQL output (set to false) -->
        <property name="hibernate.show_sql">false</property>
        <property name="hibernate.format_sql">false</property>

        <!-- Mapping annotated classes -->
        <mapping class="org.example.entity.User"/>
        <mapping class="org.example.entity.Category"/>
        <mapping class="org.example.entity.Product"/>
        <mapping class="org.example.entity.Order"/>
        <mapping class="org.example.entity.OrderItem"/>

    </session-factory>
</hibernate-configuration>

